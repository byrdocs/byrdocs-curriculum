name: Deploy guide site

on:
  push:
    branches:
      - master

jobs:
  deploy:
    name: Trigger Cloudflare Pages deploy
    runs-on: ubuntu-latest

    steps:
      - name: Trigger deploy hook
        run: curl --fail --request POST "${{ secrets.GUIDE_SITE_DEPLOY_HOOK }}"
